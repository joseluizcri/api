image: node:latest

stages:
  - build
  - test

cache:
  paths:
    - node_modules/

install:
  stage: build
  script:
    - npm install
    - npm run build
  artifacts:
    paths:
      - node_modules/
      - dist/

test:
  stage: test
  script: npm run jest